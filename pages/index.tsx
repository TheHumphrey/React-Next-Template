import React from 'react';
import Layout from '../src/components/Layout';
// import { Layout } from '@app/components/index';

const IndexPage = (): React.ReactElement => (
  <Layout title="Template Next">
    <h1>Hello Next.js 👋</h1>
  </Layout>
);

export default IndexPage;
